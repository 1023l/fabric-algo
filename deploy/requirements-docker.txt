# fabric-algo 运行时镜像依赖（Linux 容器版，对应 conda yolo-bench 环境）
# 与 requirements-yolo-bench.txt 的差异：opencv 用 headless、torch 用 Linux CUDA 轮子、
# 去掉 Windows 专属包（pyreadline3/colorama）与训练平台才需要的杂项。
# 国内镜像加速由 Dockerfile 的 PIP_INDEX_URL 环境变量提供。

# ── Web 服务 ──
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
httpx==0.28.1
starlette==1.6.0
anyio==4.14.2
h11==0.16.0

# ── TRT 推理 / ONNX ──
numpy==2.5.2
opencv-python-headless==5.0.0.93
tensorrt_cu12==11.2.1.2
tensorrt_cu12_bindings==11.2.1.2
tensorrt_cu12_libs==11.2.1.2
cupy-cuda12x==14.2.0
onnxruntime==1.29.0
onnx==1.21.0
onnx_graphsurgeon==0.6.1
polygraphy==0.53.4
pillow==12.3.0
# 与 torch==2.6.0+cu124 的 Linux 依赖对齐（torch 硬性要求 12.4.127）
nvidia-cuda-runtime-cu12==12.4.127
nvidia-ml-py==13.610.43
pyyaml==6.0.3
pydantic==2.13.4
requests==2.34.2
psutil==7.2.2

# ── det 模型 ONNX 导出（模型上传流水线用；rec 导出在训练平台侧）──
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
ultralytics==8.4.123
ultralytics-thop==2.1.6

# ── 基础 ──
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.19
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.3
python-dotenv==1.2.3
six==1.17.0
tqdm==4.70.0
typing_extensions==4.16.0
urllib3==2.7.0
filelock==3.32.0
fsspec==2026.7.0
sympy==1.13.1
networkx==3.6.1
